info:
  component: spring cloud eureka config server
eureka:
  instance:
    hostname: 192.168.99.100
    prefer-ip-address: true
  client:
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:30002/eureka/
